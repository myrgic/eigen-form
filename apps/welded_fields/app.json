{
  "id": "welded_fields",
  "title": "Welded Fields",
  "description": "Stigmergic trail crossed with procedural advection on one shared grid.",
  "version": "1.3.0",
  "kind": "frozen-golden",
  "entry": "index.html",
  "cogdoc": {
    "type": "paper",
    "id": "paper-welded-fields",
    "embedded": "script[type=\"application/cogdoc+yaml\"]",
    "claim": "With the outside noise gain at zero, the only steering field is one the agents wrote themselves (their own heading, stored in a slower momentum grid). The pieces-and-holes census is computed on the torus the agents live on, and the mask threshold is an exact coverage quantile, so the census does not change when the whole field is rescaled. Hypothesis, not tested: the momentum/trail half-life ratio governs the patterns.",
    "port": "window.__sim",
    "verify": ".cog/bin/tools/cogdoc_verify.py"
  },
  "provenance": {
    "derivesFrom": "sha256:81d49079c91d40fda49601bfe235cb9aa22598e0bae156777aa7803abf69029f",
    "changes": [
      "head, after <title>: added an embedded <script type=\"application/cogdoc+yaml\"> frontmatter block carrying the canonical cogdoc core (id `paper-welded-fields`, type `paper`, title, description, sector, status, created, updated, salience, authors, tags) plus the paper-conditional fields required by .cog/conf/spec/cogdoc-html-executable-artifacts.cog.md: `claim`, `method[]` with one entry per free parameter (all 18 sidebar sliders plus fixed N=9000, each with range, default, unit where applicable, and a MEANING — a parameter with no stated meaning is a knob, not a method), `observables` naming window.__sim and the cost of each call, `findings[]`, `limitations[]`, and typed `refs[]`. WHY: the artifact previously asserted its corrections in code comments only — line 265's 'reallocated with the grid, unlike the original' and line 625's coverage-vs-level note were unaddressable and uncountable. Spec rule 5 requires any 'unlike the original'-class comment to become a typed ref, so the physarum correction is now `refs[] uri:app:physarum rel:corrects` and is queryable as a graph edge rather than folklore. Spec rule 4 requires an artifact that makes a claim to expose and NAME a test port; window.__sim already existed (commented 'exposed for verification from the console') but was undeclared, so `observables` now names it and cogdoc_verify.py exercises it. The block is inert to the renderer (scripts of unknown type are neither executed nor rendered), so the file is simultaneously a running artifact and an indexable cogdoc. Type `paper` is value 4 of the canonical 19-type enum and is a sequenced type, hence the required `id`.",
      "version 1.0.0 -> 1.1.0: additive frontmatter only. No change to the model, its parameters, its defaults, or its rendered output — the sole edit is the inert <script> block in <head>. Declared here because this app is kind `frozen-golden` and spec rule 3 makes any edit to a frozen-golden entry a declared change with its reason. Verified after the edit: cogdoc_html.py validate -> OK [paper] paper-welded-fields with zero errors and zero warnings; headless Chromium load via cogdoc_verify.py -> canvas non-zero, lit pixels non-zero, zero console errors, and __sim.betti()/refs() checked against the stated findings.",
      "topology census made periodic (issue #38). euler() and components() (the `topology` block before measure()) read the mask as a zero-padded flat square (bit-quads over x,y in [-1,W) x [-1,H) with out-of-range cells = 0; flood fill skipped out-of-range neighbours) while the agents, trail diffusion and momentum field all wrap on a torus. Fixed: bit-quads now run over x,y in [0,W) x [0,H) with indices taken mod W,H, the 8-neighbour flood fill wraps mod W,H, and measure() and __sim.betti() use the torus relation chi = b0 - b1 + b2, i.e. b1 = b0 + b2 - euler() with b2 = 1 only when the mask covers every cell. Cogdoc observables.betti, limitations[0] and the panel note restated to match; no dynamics, parameter or default changed. Verified by driving analytic masks through the page's own measure() on the real page (605x400; torus answer / before / after, as b0,b1): empty 0,0 / 0,0 / 0,0; disk in the interior 1,0 / 1,0 / 1,0; disk centred on the corner seam 1,0 / 4,0 / 1,0; full-width band 1,1 / 1,0 / 1,1; ring crossing the x-seam 1,1 / 2,0 / 1,1; full-width + full-height cross 1,2 / 1,0 / 1,2; full mask 1,2 / 1,0 / 1,2. EFFECT ON THE HEADLINE OBSERVABLE, stated plainly: at the defaults (thr = 8% coverage) the census read the same before and after on all 5 seeds tested at 1500 frames (seeded run, before (b0,b1) = after (b0,b1) = s1 2,1; s2 2,0; s3 1,2; s4 3,0; s5 2,1) and at 2400 frames, because the swarm is seeded in a central disk and at 8% coverage the mask had no cell pair across the seam in any of those runs. The flat census only diverges once the mask touches the seam: at 20% and 40% coverage after 2400 frames, seed 1 (82 seam pairs) and seed 5 (26 seam pairs) read b0 = 2 flat vs 1 on the torus (b1 unchanged in those cases). So the paper's coverage-invariance finding is not overturned by this fix at its defaults, but b1 values measured earlier at high coverage or on long runs were counts on the square, not the torus, and are not comparable with values from this version.",
      "v1.3.0 (lane-1 correctness pass). Declared because this app is frozen-golden. (1) refs(): the coverage cut and the 99.5th-percentile ref were read off a 1024-bin LINEAR histogram up to max; with 75-93% of cells in bin 0, asking for 20-40% coverage returned 8-26%. Now exact: a log-spaced histogram (1024 bins over max*1e-7..max) finds the bin holding the k-th largest value and that bin's values are sorted to pick it. Measured on the real page after 600 frames, delivered coverage for targets 8/20/30/40%: before seed1 8.01/19.66/21.71/21.71, seed2 8.01/18.37/18.37/18.37, seed3 8.1/16.09/16.09/16.09; after 8/20/30/40 on seeds 1-3. Node unit check vs brute-force sort on 1000 synthetic fields (many zeros, 20-decade range): 0 off (before: 997 off, worst 8702 cells). (2) Momentum and hormone lattice (review F6): floor(W/8) blocks of 8 cells covered 600 of 605 columns, so the lattice had period 600 on a 605-cell torus and the last 5 columns aliased onto block 0. Now round(W/8) blocks of size W/mW (and H/mH), so the lattice tiles the torus exactly (605x400 -> 76x50 blocks of 7.96x8). This changes the dynamics slightly (block size), not the rules. (3) Text: UI labels follow wording.md 3.7 (plain words); the panel note no longer says 'verified against known shapes' without a pointer; 'governing quantity' is now an untested hypothesis (claim, method mdec, findings, refs); 'curl-noise' is now 'value-noise angle field'; `mass` is described as a saturating read gain, not a deposit strength; the spike finding now quotes the measured max/ref (1.4-6, median 1.9) instead of 'ref << max'; the coverage finding no longer says 'by construction'; the stale 'entryHash pending' limitation is removed. The periodic census (#44) is unchanged and still passes the 924-mask homology check."
    ],
    "entryHash": "sha256:93982be321387d305c8fdc16d14f26169829d3dfeddcac9d8d54c90fecfe86da"
  },
  "capabilities": {
    "emits": [],
    "accepts": []
  },
  "pwa": {
    "installable": false
  }
}
